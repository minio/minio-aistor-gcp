apiVersion: v2
name: minio-aistor
description: MinIO AIStor - High-Performance Object Storage for AI/ML Workloads (GCP Marketplace)
version: 1.0.0
appVersion: "1.0.0"
keywords:
  - storage
  - object-storage
  - minio
  - s3
  - ai
  - ml
home: https://min.io
sources:
  - https://github.com/minio/minio
maintainers:
  - name: MinIO Inc
    email: dev@min.io

dependencies:
  # Only the operator subchart - ObjectStore is deployed via custom template
  - name: aistor-operator
    version: "5.2.1"
    repository: "https://helm.min.io/"
    condition: operator.enabled
